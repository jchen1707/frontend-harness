# Claude Code compatibility

Read and follow [`AGENTS.md`](AGENTS.md). It is the canonical repository instruction file.

When you work below a directory that has its own `AGENTS.md`, read that file too. Treat each
nested `CLAUDE.md` as a compatibility pointer only.
